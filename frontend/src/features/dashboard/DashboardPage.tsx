/** Panel de inicio del estudiante.
 *
 *  Pendiente de desarrollo: sólo existe para que la ruta esté declarada y el
 *  esqueleto compile.
 */
export function DashboardPage() {
  return <section data-pending="DashboardPage" />;
}
