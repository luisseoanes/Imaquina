/** Cuentas, cursos y matrículas.
 *
 *  Pendiente de desarrollo: sólo existe para que la ruta esté declarada y el
 *  esqueleto compile.
 */
export function AdminPage() {
  return <section data-pending="AdminPage" />;
}

export default AdminPage;
