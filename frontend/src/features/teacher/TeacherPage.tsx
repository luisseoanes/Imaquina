/** Progreso por curso y tablero de resultados.
 *
 *  Pendiente de desarrollo: sólo existe para que la ruta esté declarada y el
 *  esqueleto compile.
 */
export function TeacherPage() {
  return <section data-pending="TeacherPage" />;
}

export default TeacherPage;
