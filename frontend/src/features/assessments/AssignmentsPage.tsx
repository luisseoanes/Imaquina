/** Tareas y evaluaciones del estudiante.
 *
 *  Pendiente de desarrollo: sólo existe para que la ruta esté declarada y el
 *  esqueleto compile.
 */
export function AssignmentsPage() {
  return <section data-pending="AssignmentsPage" />;
}
