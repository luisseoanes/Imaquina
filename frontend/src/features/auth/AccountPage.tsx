/** Cuenta del usuario: idioma preferido y cambio de contraseña.
 *
 *  Pendiente de desarrollo: sólo existe para que la ruta esté declarada y el
 *  esqueleto compile.
 */
export function AccountPage() {
  return <section data-pending="AccountPage" />;
}
