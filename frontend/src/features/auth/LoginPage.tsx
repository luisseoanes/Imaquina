/** Entrada a la plataforma.
 *
 *  Pendiente de desarrollo: sólo existe para que la ruta esté declarada y el
 *  esqueleto compile.
 */
export function LoginPage() {
  return <section data-pending="LoginPage" />;
}
