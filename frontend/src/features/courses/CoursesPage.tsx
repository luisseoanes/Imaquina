/** Listado de proyectos publicados.
 *
 *  Pendiente de desarrollo: sólo existe para que la ruta esté declarada y el
 *  esqueleto compile.
 */
export function CoursesPage() {
  return <section data-pending="CoursesPage" />;
}
