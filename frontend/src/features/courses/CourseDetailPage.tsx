/** Un proyecto y sus seis momentos, con el progreso.
 *
 *  Pendiente de desarrollo: sólo existe para que la ruta esté declarada y el
 *  esqueleto compile.
 */
export function CourseDetailPage() {
  return <section data-pending="CourseDetailPage" />;
}
