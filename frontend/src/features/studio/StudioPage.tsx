/** Content Studio: autoría y publicación.
 *
 *  Pendiente de desarrollo: sólo existe para que la ruta esté declarada y el
 *  esqueleto compile.
 */
export function StudioPage() {
  return <section data-pending="StudioPage" />;
}

export default StudioPage;
