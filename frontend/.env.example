# Base de la API. En desarrollo se deja vacia: el proxy de Vite manda /api al
# backend y asi no hay CORS ni URLs absolutas por entorno.
VITE_API_BASE_URL=/api/v1

# A donde apunta el proxy de Vite. Dentro de docker-compose seria http://api:8000
VITE_API_PROXY_TARGET=http://localhost:8000
